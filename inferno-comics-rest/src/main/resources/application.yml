app:
  version: 0.5.3

server:
  servlet:
    contextPath: /${URL_PREFIX}

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

spring:
  application:
    name: inferno-comics-rest
  profiles:
    active: local